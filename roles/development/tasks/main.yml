---
- include: pacman.yml
  become: yes
  become_method: sudo
  tags:
    - packages

- include: node.yml
  become: yes
  become_method: sudo
  tags:
    - node

- include: ruby.yml
  become: yes
  become_method: sudo
  tags:
    - ruby

- include: haskell.yml
  become: yes
  become_method: sudo
  tags:
    - haskell

- include: docker.yml
  become: yes
  become_user: root
  tags:
    - docker

- include: postgres.yml
  become: yes
  become_user: root
  tags:
    - postgres

- include: zsh.yml
  become_user: "{{ username }}"
  tags:
    - zsh

- include: vim.yml
  become_user: "{{ username }}"
  tags:
    - vim

- include: watchman.yml
  become_user: "{{ username }}"
  tags:
    - watchman

- include: emacs.yml
  become_user: "{{ username }}"
  tags:
    - emacs

- include: elixir.yml
  become_user: "{{ username }}"
  tags:
    - elixir
